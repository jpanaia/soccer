/*
 * This is a manifest file that'll be compiled into application.css, which will include all the files
 * listed below.
 *
 * Any CSS and SCSS file within this directory, lib/assets/stylesheets, vendor/assets/stylesheets,
 * or vendor/assets/stylesheets of plugins, if any, can be referenced here using a relative path.
 *
 * You're free to add application-wide styles to this file and they'll appear at the bottom of the
 * compiled file so the styles you add here take precedence over styles defined in any styles
 * defined in the other CSS/SCSS files in this directory. It is generally better to create a new
 * file per style scope.
 *
 *= require_tree .
 *= require_self
 */

body {
  color: #fff;
  background: url(<%= asset_path 'soccer-bg.jpg' %>) no-repeat 0 25% fixed;
  background-size: cover;
  height: 100%;
}

#wrapper::after{
 content: "";
 background: url(<%= asset_path 'grunge.jpg' %>);
 opacity: 0.3;
 top: 0;
 left: 0;
 bottom: 0;
 right: 0;
 position: fixed;
 z-index: -1;  
 height: 100%;
}

 h1, h2, h3 {
	font-family: 'Dosis', sans-serif; 
	font-weight:700;
}

input {
	color: #000;
}
/* .row {
 	color: #595959;
 	background: #fff;
 }*/

 .navbar {
 	border-bottom: 2px solid #120;
 	min-height: 70px;
 	background: url(<%= asset_path 'nav-grunge.jpg'%>);
 }


 .navbar-nav>li>a {
	padding-top: 25px;
	padding-bottom: 25px;
	font-size: 20px;
	color: #fff;
	font-family: 'Slabo 27px', serif;
	}
.navbar-nav>li>a:hover {
	background: #120;
}

.navbar-right {
	margin-top: 20px;
	font-family: 'Slabo 27px', serif;
	font-size: 18px;
}
  .navbar-right a, .navbar-right a:hover, .navbar-right a:visited{
  	color: #25c4d4;

  }

#article {
	background: #fff;
	color: #595959;
}
 .ch-item {
    width: 100%;
    height: 100%;
    border-radius: 50%;
    position: relative;
    box-shadow: 0 1px 2px rgba(0,0,0,0.1);
    cursor: default;
}

.ch-grid {
    margin: 20px 0 0 0;
    padding: 0;
    list-style: none;
    display: block;
    text-align: center;
    width: 100%;
}
  .ch-grid a, .ch-grid a:hover, .ch-grid a:active, .ch-grid a:visited {
   color: #fff;
  }
 
.ch-grid:after,
.ch-item:before {
    content: '';
    display: table;
}
 
.ch-grid:after {
    clear: both;
}
 
.ch-grid li {
    width: 220px;
    height: 220px;
    display: inline-block;
    margin: 20px;
}

.ch-info-wrap{
    position: absolute;
    width: 180px;
    height: 180px;
    border-radius: 50%;
    perspective: 800px;
    transition: all 0.4s ease-in-out;
    top: 20px;
    left: 20px;
    background: #000;
    box-shadow: 
        0 0 0 20px rgba(0,0,0,0.5), 
        inset 0 0 3px rgba(115,114, 23, 0.8);
 
}

.ch-info{
    position: absolute;
    width: 180px;
    height: 180px;
    border-radius: 50%;
    transition: all 0.4s ease-in-out;
    transform-style: preserve-3d;
}

.ch-info > div {
    display: block;
    position: absolute;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    background-position: center center;
    backface-visibility: hidden;
}

.ch-info .ch-info-back {
    transform: rotate3d(0,1,0,180deg);
    background: rgba(0,0,0,0.9);
}

.ch-info h3 {
    color: #fff;
    text-transform: uppercase;
    letter-spacing: 2px;
    font-size: 14px;
    margin: 0 15px;
    padding: 40px 0 0 0;
    height: 90px;
    font-family: 'Open Sans', Arial, sans-serif;
    text-shadow: 
        0 0 1px #fff, 
        0 1px 2px rgba(0,0,0,0.3);
}
 
.ch-info p {
    color: #fff;
    padding: 10px 5px;
    font-style: italic;
    margin: 0 30px;
    font-size: 12px;
    border-top: 1px solid rgba(255,255,255,0.5);
}
 
.ch-info p a {
    display: block;
    color: rgba(255,255,255,0.7);
    font-style: normal;
    font-weight: 700;
    text-transform: uppercase;
    font-size: 10px;
    letter-spacing: 1px;
    padding-top: 4px;
    font-family: 'Open Sans', Arial, sans-serif;
}
 
.ch-info p a:hover {
    color: rgba(255,242,34, 0.8);
}

.ch-item:hover .ch-info-wrap {
    box-shadow: 
        0 0 0 0 rgba(255,255,255,0.8), 
        inset 0 0 3px rgba(0,0,0, 0.8);
}
 
.ch-item:hover .ch-info {
    transform: rotate3d(0,1,0,-180deg);
}

